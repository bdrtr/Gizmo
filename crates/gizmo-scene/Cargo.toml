[package]
name = "gizmo-scene"
version.workspace = true
edition.workspace = true

[dependencies]
gizmo-core = { path = "../gizmo-core" }
gizmo-math = { path = "../gizmo-math" }
gizmo-physics = { path = "../gizmo-physics" }
gizmo-renderer = { path = "../gizmo-renderer" }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
wgpu = "0.19"
