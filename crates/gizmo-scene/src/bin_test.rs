use gizmo_core::World;
use gizmo_scene::registry::SceneRegistry;
use gizmo_scene::scene::SceneData;
use std::sync::Arc;

fn main() {
    println!("Testing scene save & load pipeline...");
    // We cannot run wgpu easily in terminal without a window / setup, so let's just make sure we are not crashing...
}
